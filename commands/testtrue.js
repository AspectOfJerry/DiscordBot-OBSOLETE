module.exports = {
    name: 'testrue',
    cooldown: 10,
    description: "Usage: %testtrue",
    execute(message, args, cmd, client, Discord){
  
      message.reply('There is currently nothing to test!');
    }
}